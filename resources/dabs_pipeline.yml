# The main pipeline for dabs
resources:
  pipelines:
    dabs_pipeline:
      name: dabs_pipeline
      catalog: main
      schema: dabs_pavan_${bundle.environment}
      libraries:
        - notebook:
            path: ../src/ldp_pipeline.ipynb

      configuration:
        bundle.sourcePath: ${workspace.file_path}/src
