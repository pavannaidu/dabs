# The main pipeline for dabs
resources:
  pipelines:
    dabs_pipeline:
      name: dabs_pipeline
      target: dabs_${bundle.environment}
      libraries:
        - notebook:
            path: ../src/dlt_pipeline.ipynb

      configuration:
        bundle.sourcePath: /Workspace/${workspace.file_path}/src
